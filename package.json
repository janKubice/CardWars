{
  "name": "cardwars",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CardWars – roguelike deckbuilder na mřížce (web MVP)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "node --experimental-strip-types scripts/smoke.ts",
    "runsmoke": "node --experimental-strip-types scripts/runsmoke.ts",
    "featcheck": "node --experimental-strip-types scripts/featcheck.ts"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^8.1.3",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
